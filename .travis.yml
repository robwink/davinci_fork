sudo: required
dist: trusty
language: c
compiler:
  - gcc
  - clang
# Change this to your needs


addons:
  apt:
    sources:
    packages:
      - python3
      - libcfitsio3-dev
      - libhdf5-dev # in newer releases
        #- libhdf5-serial-dev #on precise
      - gnuplot

    #branches:
    #except:
    #- header_madness

script:
  - ./configure --disable-jbig && make
    #the next 2 steps shouldn't be necessary but oh well ...
  - make davinci.sh
  - . davinci.sh
  - cd dv_tests
  - export LD_LIBRARY_PATH=../.libs/
  #- sudo make install
  #- ./run_tests.py -d ../.libs/davinci -t .
  - ./run_tests.py -d ../.libs/davinci

